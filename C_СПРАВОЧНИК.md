# C — справочник (базовый)

Язык **C** на бассейне School 21: **C11**, **GCC**, стиль Google.  
Не путать с **shell** (`.sh`) и **C++** (`.cpp`).

Связанные: [BASH_СПРАВОЧНИК](BASH_СПРАВОЧНИК.md) (компиляция), [T03D03 разбор](T03D03_РАЗБОР_ЗАДАНИЯ.md).

---

## Как пополнять

1. Раздел или новая строка в таблице.
2. **конструкция → зачем → пример**.
3. Запись в **Журнал**.

---

## C vs Shell vs C++

| | C (`.c`) | Shell (`.sh`) | C++ (`.cpp`) |
|---|----------|---------------|--------------|
| Запуск | компиляция + `./prog` | `bash script.sh` | `g++` + `./prog` |
| Точка входа | `int main(void)` | нет main | `int main()` |
| Типизация | строгая | нет | строгая + ООП |

---

## Минимальная программа

```c
#include <stdio.h>

int main(void) {
  printf("Hello, AI!");
  return 0;
}
```

| Элемент | Зачем |
|---------|--------|
| `#include <stdio.h>` | Подключить printf, scanf |
| `int main(void)` | Точка входа, 0 = успех |
| `return 0;` | Код возврата ОС |

---

## Компиляция (GCC)

```bash
gcc -std=c11 -Wall -Werror -Wextra src/hello.c -o hello
./hello
```

С `math.h`:

```bash
gcc -std=c11 -Wall -Werror -Wextra src/important_function.c -o prog -lm
```

| Флаг | Зачем |
|------|--------|
| `-std=c11` | Стандарт C11 |
| `-Wall -Werror -Wextra` | Все предупреждения = ошибки |
| `-o имя` | Имя исполняемого файла |
| `-lm` | Подключить libm (pow, sqrt, fabs…) |

**Не пушить в git:** `a.out`, `*.o`, бинарники — только `.c`.

---

## Типы данных

| Тип | Пример | scanf | printf |
|-----|--------|-------|--------|
| `int` | `42` | `%d` | `%d` |
| `double` | `3.14` | `%lf` | `%f`, `%.1f` |
| `float` | `1.5f` | `%f` | `%f` |
| `char` | `'a'` | `%c` | `%c` |

```c
int a = 0;
double x = 0.0;
scanf("%d", &a);      // & — адрес переменной
scanf("%lf", &x);
```

---

## Ввод / вывод (`stdio.h`)

| Функция | Зачем | Пример |
|---------|--------|--------|
| `printf("fmt", ...)` | Вывод | `printf("%d\n", a);` |
| `scanf("fmt", &var)` | Ввод | `scanf("%d %d", &a, &b);` |
| `return scanf(...)` | Сколько полей считано | `if (scanf("%d", &a) != 1)` |

### Проверка ввода (шаблон)

```c
int a = 0, b = 0;
if (scanf("%d %d", &a, &b) != 2) {
  printf("n/a");
  return 0;
}
```

Для float/double — `%lf`. Если ввели `12.3` в `%d` — `scanf` вернёт не 2 → `n/a`.

### Форматы printf

| Спецификатор | Смысл |
|--------------|--------|
| `%d` | целое |
| `%f` | float/double |
| `%.1f` | 1 знак после запятой |
| `%s` | строка |
| `\n` | перевод строки (если нужен) |

**Внимание:** в T03 часто **без** `\n` в конце — читай условие и тесты.

---

## Операторы

| Оператор | Пример |
|----------|--------|
| арифметика | `+ - * / %` |
| целочисленное деление | `7 / 2` → `3` |
| сравнение | `== != < > <= >=` |
| логика | `&& \|\| !` |
| тернарный | `a >= b ? a : b` |

---

## Условия

```c
if (b == 0) {
  printf("n/a");
} else {
  int q = a / b;
  printf("%d", q);
}
```

```c
if (x * x + y * y < 25.0)
  printf("GOTCHA");
else
  printf("MISS");
```

---

## Функции

```c
int max2(int a, int b);

int main(void) {
  printf("%d", max2(3, 2));
  return 0;
}

int max2(int a, int b) {
  return a >= b ? a : b;
}
```

| Правило | Смысл |
|---------|--------|
| объявление до вызова | прототип `int max2(int a, int b);` |
| тип возврата | `int`, `double`, `void` |
| `void` в параметрах | `int main(void)` — без аргументов |

---

## `math.h`

```c
#include <math.h>
```

| Функция | Зачем | Пример |
|---------|--------|--------|
| `pow(a, b)` | степень | `pow(x, 4)` |
| `sqrt(x)` | корень | `sqrt(25)` |
| `fabs(x)` | модуль float/double | `fabs(res)` |
| `isnan(x)` | не число | проверка ошибки |
| `isinf(x)` | бесконечность | проверка ошибки |

Компиляция: **`-lm`** в конце команды gcc.

Кубический корень: `pow(x, 1.0 / 3.0)`.

---

## Float и epsilon (Quest 6)

**Нельзя:** `if (x == 0.0)` после вычислений.

**Нужно:**

```c
#include <math.h>

#define EPSILON 1e-6

if (fabs(res) < EPSILON)
  printf("OK!");
```

Погрешность float/double — сравнивать с маленьким порогом.

---

## Комментарии

```c
/* многострочный
   комментарий */

// однострочный (C99+)
```

---

## Стиль (кратко, Google / бассейн)

- отступ **4 пробела** (или 2 — как в репо)
- `{` на той же строке что `if`/`function`
- имена: `snake_case` для функций/переменных
- не оставлять мусорный код и лишние комментарии
- `return 0;` в конце `main`

---

## Частые ошибки

| Ошибка | Правильно |
|--------|-----------|
| `scanf("%d", a)` | `scanf("%d", &a)` |
| забыли `-lm` | `gcc ... -lm` |
| `printf("%d ", x);` лишний пробел в конце | без пробела после последнего числа |
| `a / b` при b=0 | проверка → `n/a` |
| `== 0.0` для float | `fabs(x) < EPSILON` |
| пушить `a.out` | только `.c` в git |
| `void main()` | `int main(void)` |

---

## Шаблоны по квестам T03

### hello.c

```c
#include <stdio.h>
int main(void) {
  printf("Hello, AI!");
  return 0;
}
```

### arithmetic.c (4 числа через пробел)

```c
printf("%d %d %d %d", sum, diff, prod, quot);
// или с n/a при b==0
```

### crack.c (круг r=5)

```c
if (x * x + y * y < 25.0)
  printf("GOTCHA");
```

---

## Термины

| Термин | Смысл |
|--------|--------|
| Компилятор | Переводит `.c` в машинный код |
| Линковка | Сборка с библиотеками (`-lm`) |
| Заголовок `.h` | Объявления (stdio.h, math.h) |
| Прототип функции | Объявление до определения |
| Литерал | `42`, `3.14`, `"text"` |
| undefined behavior | Поведение не определено (деление на 0) |

---

## Журнал пополнений

| Дата | Что добавили |
|------|--------------|
| 2026-07-13 | База C11: main, типы, scanf/printf, функции, math.h, epsilon, gcc |
